server:
  port: 9954
spring:
  profiles:
    active: dev
  application:
    name: micro-knife4j-order
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"


